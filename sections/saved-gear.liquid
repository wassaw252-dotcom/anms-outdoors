<section class="section wrap"><span class="eyebrow">{{ 'saved.eyebrow' | t }}</span><h1>{{ 'nav.saved' | t }}</h1><p class="muted">{{ 'saved.device' | t }}</p><div class="product-grid" data-saved-grid><div class="empty-state"><h2>{{ 'saved.empty' | t }}</h2><p>{{ 'saved.empty_copy' | t }}</p><a class="button" href="{{ routes.all_products_collection_url }}">{{ 'nav.all_gear' | t }}</a></div></div><noscript><p>{{ 'saved.nojs' | t }}</p></noscript></section>
{% schema %}
{"name":"Saved gear","settings":[]}
{% endschema %}
